import 'package:flutter/material.dart';

class ReceiptTextExtractor extends StatelessWidget {
  const ReceiptTextExtractor({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Container(
        child: const Text('Receipt Text Extractor'),
      ),
    );
  }
}
